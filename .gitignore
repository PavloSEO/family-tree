node_modules
dist
build
.env
.env.*.local
pnpm-debug.log
*.log
data/
.DS_Store
Thumbs.db

# Не коммитить: архивы логов, каталог этапов, локальная копия material-web (справка; в коде — npm-пакет @material/web)
log/
log.zip
material-web-main/

# Локальный архив с чужим примером nginx (Next/Strapi) — не нужен для сборки
nginx.zip
